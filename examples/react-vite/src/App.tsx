export function App() {
  return (
    <main className="card">
      <span className="eyebrow">Real-project example</span>
      <h1>Govern components without guessing</h1>
      <p>Scan the native elements, preview a safe fix, and let Component Vault add only proven imports.</p>
      <button type="button">Review the dry-run</button>
    </main>
  );
}
